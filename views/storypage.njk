{% extends "layout.njk" %}

{% block content%}
    <p>{{page.message}}</p>
    <ul>
        {% for choice in page.choice %}
            <li><a href="/story/{{choice.targetId}}">{{choice.description}}</a></li>
        {% else %}
            <li>Det finns inget val</li>
        {% endfor %}
    </ul>
{% endblock %}